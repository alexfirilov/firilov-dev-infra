apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: sonarr
  namespace: flux-system
spec:
  interval: 24h
  url: https://charts.pree.dev
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarr
  namespace: flux-system
spec:
  releaseName: sonarr
  targetNamespace: sonarr
  install:
    createNamespace: true
  interval: 30m
  chart:
    spec:
      chart: sonarr
      sourceRef:
        kind: HelmRepository
        name: sonarr
        namespace: flux-system
  values:
    image:
      tag: 4.0.16
    env:
      TZ: Asia/Jerusalem
    # Match qBittorrent's uid/gid so imports hardlink instead of copy.
    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    persistence:
      config:
        enabled: true
        retain: true
        accessMode: ReadWriteOnce
        size: 1Gi

      media:
        enabled: true
        existingClaim: media
        mountPath: /mnt/media
